//used for sharing code in another document without needing to rewrite it
const uppercaseString = (string) => {
    return string.toUpperCase();
  }
  
  const lowercaseString = (string) => {
    return string.toLowerCase()
  }
  export {uppercaseString, lowercaseString};//used when you need to export more than 1 variable